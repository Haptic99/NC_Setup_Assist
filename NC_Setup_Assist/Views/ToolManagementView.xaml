<UserControl x:Class="NC_Setup_Assist.Views.ToolManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:NC_Setup_Assist.Views"
             xmlns:vm="clr-namespace:NC_Setup_Assist.ViewModels"
             xmlns:conv="clr-namespace:NC_Setup_Assist.Converters"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <conv:BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <conv:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVis"/>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <DockPanel Grid.Column="0" Margin="0,0,10,0">
            <TextBox DockPanel.Dock="Top" Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}" Margin="5,0,5,5" ToolTip="Suche nach Name, Typ oder Beschreibung..."/>

            <ListView ItemsSource="{Binding FilteredWerkzeuge}"
                      SelectedItem="{Binding SelectedTool, Mode=TwoWay}"
                      IsEnabled="{Binding IsInEditMode, Converter={StaticResource InverseBoolToVis}}">

                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="MouseDoubleClick">
                        <i:InvokeCommandAction Command="{Binding EditToolCommand}" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>

                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="ID" DisplayMemberBinding="{Binding WerkzeugID}" Width="50"/>
                        <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="150"/>
                        <GridViewColumn Header="Typ" DisplayMemberBinding="{Binding Unterkategorie.Name}" Width="150"/>
                        <GridViewColumn Header="Beschreibung" DisplayMemberBinding="{Binding Beschreibung}" Width="Auto"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </DockPanel>

        <Grid Grid.Column="1" Visibility="{Binding IsSelectionMode, Converter={StaticResource InverseBoolToVis}}">
            <StackPanel Visibility="{Binding IsInEditMode, Converter={StaticResource BoolToVis}}">
                <TextBlock Text="Werkzeug Details" FontWeight="Bold" Margin="0,0,0,10"/>
                <TextBlock Text="Name:"/>
                <TextBox Text="{Binding EditingTool.Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5"/>
                <TextBlock Text="Kategorie:"/>
                <ComboBox Margin="0,0,0,5"
                          ItemsSource="{Binding Kategorien}"
                          SelectedItem="{Binding SelectedKategorie}"
                          DisplayMemberPath="Name"/>
                <TextBlock Text="Unterkategorie / Typ:"/>
                <ComboBox Margin="0,0,0,5"
                          ItemsSource="{Binding Unterkategorien}"
                          SelectedItem="{Binding EditingTool.Unterkategorie}"
                          DisplayMemberPath="Name"/>
                <TextBlock Text="Beschreibung:"/>
                <TextBox Text="{Binding EditingTool.Beschreibung, UpdateSourceTrigger=PropertyChanged}" Height="80" TextWrapping="Wrap" Margin="0,0,0,10" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
                <Button Content="Speichern" Command="{Binding SaveToolCommand}" Margin="0,5,0,0"/>
                <Button Content="Abbrechen" Command="{Binding CancelCommand}" Margin="0,5,0,0"/>
            </StackPanel>

            <StackPanel VerticalAlignment="Top"
                        Visibility="{Binding IsInEditMode, Converter={StaticResource InverseBoolToVis}}">
                <Button Content="Neues Werkzeug anlegen" Command="{Binding NewToolCommand}" Margin="0,5,0,0"/>
                <Button Content="Werkzeug löschen" Command="{Binding DeleteToolCommand}" Margin="0,5,0,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>