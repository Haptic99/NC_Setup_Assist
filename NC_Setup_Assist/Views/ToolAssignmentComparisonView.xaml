<UserControl x:Class="NC_Setup_Assist.Views.ToolAssignmentComparisonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:NC_Setup_Assist.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance vm:ToolAssignmentComparisonViewModel}"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Werkzeugzuweisung Vergleich" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <ListView Grid.Row="1" ItemsSource="{Binding ComparisonItems}" AlternationCount="2">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Style.Triggers>
                        <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                            <Setter Property="Background" Value="#EFEFEF"/>
                        </Trigger>
                        <DataTrigger Binding="{Binding ToolNameAfter}" Value="Unzugewiesen">
                            <Setter Property="Background" Value="#FFDDDD"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Station" DisplayMemberBinding="{Binding Station}" Width="80"/>
                    <GridViewColumn Header="Korrektur" DisplayMemberBinding="{Binding Korrektur}" Width="80"/>
                    <GridViewColumn Header="Werkzeug (Vorher / Standard)" DisplayMemberBinding="{Binding ToolNameBefore}" Width="300"/>
                    <GridViewColumn Header="Werkzeug (Nachher / Zugewiesen)" DisplayMemberBinding="{Binding ToolNameAfter}" Width="300"/>
                </GridView>
            </ListView.View>
        </ListView>

        <Button Grid.Row="2" Content="Schliessen" 
                Command="{Binding CloseCommand}" 
                HorizontalAlignment="Right" 
                Margin="0,10,0,0" 
                Width="100"/>
    </Grid>
</UserControl>