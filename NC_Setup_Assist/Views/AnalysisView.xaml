<UserControl x:Class="NC_Setup_Assist.Views.AnalysisView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NC_Setup_Assist.Views"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
     
         d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Analyse &amp; Bearbeitung" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <Grid Grid.Row="1">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <DockPanel Grid.Column="0" Margin="0,0,5,0">
                <TextBlock DockPanel.Dock="Top" Text="NC-Code" 
 
                           FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBox x:Name="NcCodeTextBox" 
                         Text="{Binding NcCodeContent}"
                         IsReadOnly="True"
   
                       AcceptsReturn="True"
                         FontFamily="Consolas"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"/>

            </DockPanel>

            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="LightGray"/>

            <DockPanel Grid.Column="2" Margin="5,0,0,0">
                <TextBlock DockPanel.Dock="Top" Text="Gefundene Werkzeugeinsätze" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <ListView x:Name="WerkzeugEinsaetzeListView" ItemsSource="{Binding WerkzeugEinsaetze}" AlternationCount="2">

                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="MouseDoubleClick">
                            <i:InvokeCommandAction Command="{Binding GoToToolInCodeCommand}"
                                            
                                     CommandParameter="{Binding ElementName=WerkzeugEinsaetzeListView, Path=SelectedItem}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">

                            <Style.Triggers>
                                <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                                    <Setter Property="Background" Value="#EFEFEF"/>

                                </Trigger>
                            </Style.Triggers>
                        </Style>

                    </ListView.ItemContainerStyle>
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Nr."
 DisplayMemberBinding="{Binding Reihenfolge}" Width="30"/>
                            <GridViewColumn Header="X" DisplayMemberBinding="{Binding Anzahl}" Width="30"/>
                            <GridViewColumn Header="Station" DisplayMemberBinding="{Binding RevolverStation}" Width="50"/>
                            <GridViewColumn Header="Korrektur" DisplayMemberBinding="{Binding KorrekturNummer}" Width="60"/>

                            <GridViewColumn Header="Werkzeug" DisplayMemberBinding="{Binding ZugehoerigesWerkzeug.Name}" Width="200"/>
                            <GridViewColumn Header="Kommentar" DisplayMemberBinding="{Binding Kommentar}" Width="Auto"/>
                        </GridView>

                    </ListView.View>
                </ListView>
            </DockPanel>
        </Grid>

        <Button Grid.Row="2" 
                Content="Werkzeugzuweisung starten" 
                Command="{Binding StartToolAssignmentCommand}"
                IsEnabled="{Binding CanStartToolAssignment}"
                HorizontalAlignment="Right" 
                Margin="0,10,0,0" 
                ToolTip="Startet den sequenziellen Zuweisungsprozess für alle im Code gefundenen, unzugewiesenen Werkzeuge."/>

    </Grid>
</UserControl>