<UserControl x:Class="NC_Setup_Assist.Views.ProjectManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NC_Setup_Assist.Views"
             xmlns:models="clr-namespace:NC_Setup_Assist.Models"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Projekte verwalten" 
                   FontSize="18" 
                   FontWeight="Bold" 
                   Margin="0,0,0,10"
                   Foreground="#333333"/>

        <ListView Grid.Row="1" 
                  ItemsSource="{Binding Projekte}"
                  SelectedItem="{Binding SelectedProjekt, Mode=TwoWay}"
                  Margin="0,0,0,10"
                  AlternationCount="2"
                  x:Name="ProjectsListView">

            <i:Interaction.Triggers>
                <i:EventTrigger EventName="MouseDoubleClick">
                    <i:InvokeCommandAction Command="{Binding OpenProjectCommand}"
                                           CommandParameter="{Binding ElementName=ProjectsListView, Path=SelectedItem}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>

            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Padding" Value="5"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    <Setter Property="BorderBrush" Value="#EEEEEE"/>
                    <Style.Triggers>
                        <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                            <Setter Property="Background" Value="#F8F8F8"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#E0F0FF"/>
                            <Setter Property="Foreground" Value="Black"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Projektname" DisplayMemberBinding="{Binding Name}" Width="200"/>
                    <GridViewColumn Header="Maschine" DisplayMemberBinding="{Binding ZugehoerigeMaschine.Name}" Width="150"/>
                    <GridViewColumn Header="NC-Programme" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate DataType="{x:Type models:Projekt}">
                                <TextBlock Text="{Binding NCProgramme.Count}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="ID" DisplayMemberBinding="{Binding ProjektID}" Width="100"/>
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Öffnen" Command="{Binding OpenProjectCommand}" 
                    CommandParameter="{Binding SelectedProjekt}" 
                    Style="{StaticResource AccentButton}"
                    Width="100" Margin="0,0,10,0" IsDefault="True"/>
            <Button Content="Löschen" 
                    Command="{Binding DeleteProjectCommand}" 
                    Style="{StaticResource SecondaryButton}"
                    Width="100"/>
        </StackPanel>
    </Grid>
</UserControl>