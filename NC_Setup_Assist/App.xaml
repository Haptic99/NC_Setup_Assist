<Application x:Class="NC_Setup_Assist.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:NC_Setup_Assist">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Light.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign2.Defaults.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignColors;component/Themes/MaterialDesignColor.Indigo.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignColors;component/Themes/MaterialDesignColor.Lime.xaml" />
                <ResourceDictionary Source="/Resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- Custom Brushes mapped to Material Design -->
            <SolidColorBrush x:Key="SidebarBackgroundBrush" Color="{StaticResource Primary700}"/>
            <SolidColorBrush x:Key="SidebarForegroundBrush" Color="{StaticResource PrimaryForeground}"/>
            
            <!-- Global Text Styles -->
            <Style x:Key="PageHeaderStyle" TargetType="{x:Type TextBlock}" BasedOn="{StaticResource MaterialDesignHeadline4}">
                <Setter Property="Margin" Value="0,0,0,20"/>
                <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/>
            </Style>

            <Style x:Key="SectionHeaderStyle" TargetType="{x:Type TextBlock}" BasedOn="{StaticResource MaterialDesignHeadline6}">
                <Setter Property="Margin" Value="0,10,0,10"/>
                <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/>
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>